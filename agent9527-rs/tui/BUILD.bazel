load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "agent9527_rust_crate")

agent9527_rust_crate(
    name = "tui",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ) + [
        "//agent9527-rs/collaboration-mode-templates:templates/default.md",
        "//agent9527-rs/collaboration-mode-templates:templates/plan.md",
    ],
    crate_name = "agent9527_tui",
    extra_binaries = [
        "//agent9527-rs/cli:agent9527",
    ],
    integration_compile_data_extra = ["src/test_backend.rs"],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
    ]),
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
)
