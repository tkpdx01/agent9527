load("//:defs.bzl", "agent9527_rust_crate")

agent9527_rust_crate(
    name = "app-server-protocol",
    crate_name = "agent9527_app_server_protocol",
    test_data_extra = glob(
        ["schema/**"],
        allow_empty = True,
    ),
)
