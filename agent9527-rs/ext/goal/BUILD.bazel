load("//:defs.bzl", "agent9527_rust_crate")

agent9527_rust_crate(
    name = "goal",
    compile_data = glob([
        "templates/**",
    ]),
    crate_name = "agent9527_goal_extension",
    integration_compile_data_extra = ["src/accounting.rs"],
)
