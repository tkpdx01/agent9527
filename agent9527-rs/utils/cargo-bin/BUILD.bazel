load("//:defs.bzl", "agent9527_rust_crate")

exports_files(
    ["repo_root.marker"],
    visibility = ["//visibility:public"],
)

agent9527_rust_crate(
    name = "cargo-bin",
    compile_data = ["repo_root.marker"],
    crate_name = "agent9527_utils_cargo_bin",
    lib_data_extra = ["repo_root.marker"],
    rustc_env = {
        "AGENT9527_REPO_ROOT_MARKER": "$(rlocationpath :repo_root.marker)",
    },
    test_data_extra = ["repo_root.marker"],
)
