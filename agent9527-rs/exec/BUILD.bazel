load("//:defs.bzl", "agent9527_rust_crate")

agent9527_rust_crate(
    name = "exec",
    crate_name = "agent9527_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
