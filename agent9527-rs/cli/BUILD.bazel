load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "agent9527_rust_crate")
load("//bazel/platforms:release_binaries.bzl", "multiplatform_binaries")
load("//bazel/rules:e2e_benchmark.bzl", "agent9527_e2e_benchmark")

agent9527_rust_crate(
    name = "cli",
    crate_name = "agent9527_cli",
    extra_binaries = [
        "//agent9527-rs/bwrap:bwrap",
    ],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_data_extra = glob(["src/**/snapshots/**"]),
)

multiplatform_binaries(
    name = "agent9527",
)

agent9527_e2e_benchmark(
    name = "agent9527-help",
    binaries = [":agent9527"],
)
