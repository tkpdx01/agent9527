[build-system]
requires = ["uv_build>=0.11.19,<0.12"]
build-backend = "uv_build"

[project]
name = "openai-agent9527"
version = "20260727.1.0"
description = "Python SDK for Agent9527"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
authors = [{ name = "OpenAI" }]
keywords = ["agent9527", "sdk", "llm", "ai", "agents"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = ["pydantic>=2.12", "openai-agent9527-cli-bin==20260727.1.0"]

[project.urls]
Homepage = "https://github.com/tkpdx01/agent9527"
Repository = "https://github.com/tkpdx01/agent9527"
Issues = "https://github.com/tkpdx01/agent9527/issues"
Documentation = "https://github.com/tkpdx01/agent9527/tree/main/sdk/python/docs"

[dependency-groups]
format = ["ruff>=0.15.8"]
test = [
  "pytest>=8.0",
  "datamodel-code-generator==0.31.2",
  { include-group = "format" },
]
dev = [
  { include-group = "test" },
  { include-group = "format" },
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
required-version = ">=0.15.8"
line-length = 100
extend-exclude = [
  "notebooks/**",
  "src/openai_agent9527/generated/**",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "C4"]
ignore = ["E501"]
preview = true
extend-safe-fixes = ["ALL"]
unfixable = ["F841"]

[tool.ruff.lint.per-file-ignores]
"examples/**/*.py" = ["E402"]
"tests/test_real_app_server_integration.py" = ["E402"]

[tool.ruff.lint.isort]
combine-as-imports = true

[tool.uv]
exclude-newer = "7 days"
exclude-newer-package = { openai-agent9527-cli-bin = "2026-07-15T01:00:00Z" }
index-strategy = "first-index"

[tool.uv.sources]
openai-agent9527-cli-bin = { path = "../python-runtime" }

[tool.uv.pip]
exclude-newer = "7 days"
exclude-newer-package = { openai-agent9527-cli-bin = "2026-07-15T01:00:00Z" }
index-strategy = "first-index"
